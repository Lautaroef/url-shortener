{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "Enterprise-grade URL shortener with Next.js and NestJS",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run -r build",
    "start": "pnpm run --parallel start",
    "lint": "pnpm run -r lint",
    "format": "pnpm run -r format"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}