{
  "version": 2,
  "buildCommand": "pnpm run build && cp -r dist api/dist && cp -r prisma api/prisma",
  "outputDirectory": "api",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}